<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gseii2.smartmedical">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true">
        <activity android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" android:theme="@style/LoginTheme"/>
        <activity android:name=".SignUpActivity" android:theme="@style/LoginTheme"/>
        <activity android:name=".UserInfoActivity" android:theme="@style/UserInfo"/>
        <activity android:name=".TempActivity" android:theme="@style/SplashTheme"/>
        <activity android:name=".BpmActivity" android:theme="@style/SplashTheme"/>
        <activity android:name=".ProfileActivity" android:theme="@style/SplashTheme"/>
    </application>

</manifest>